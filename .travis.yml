language: ruby

rvm:
  - 1.9.3
  - 2.0.0
  - 2.1.1
  - ruby-head

notifications:
  disabled: true

script:
  - bundle exec rspec

matrix:
  allow_failures:
    - rvm: ruby-head

before_script:
  - bundle exec rake compile
